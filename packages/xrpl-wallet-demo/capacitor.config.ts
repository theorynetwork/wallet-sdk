import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'xrpl-wallet-demo',
  webDir: '../../dist/packages/xrpl-wallet-demo',
  bundledWebRuntime: false,
};

export default config;
